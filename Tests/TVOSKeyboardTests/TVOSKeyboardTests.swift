import XCTest
@testable import TVOSKeyboard

final class TVOSKeyboardTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TVOSKeyboard().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
